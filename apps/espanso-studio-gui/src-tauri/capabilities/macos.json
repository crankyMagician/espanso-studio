{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability-macos",
  "description": "Capabilities for the main window on macOS",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
